<div class="overlay-a">
  <div class="container">
    <div class="row">
      <div class="col-sm-12">
        <ul class="nav nav-pills nav-pills-center">
          <li role="presentation" class="active"><%= link_to "All Restaurants", root_path %></li>
          <% @categories.each do |category| %>
            <li role="presentation" class="nav-list"><%= link_to category.name, category_path(category) %></li>
          <% end %>
        </ul>
        <hr>
        

        <%= render partial: "shared/restaurant_list" %>

      </div>

      <div class="text-center">
        <%= paginate @restaurants %>
      </div>
    </div>
  </div>
</div>